services:
  - type: web
    name: dash-biblia
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:server"
    envVars:
      - key: DASH_DEBUG
        value: 'False'
    plan: free
